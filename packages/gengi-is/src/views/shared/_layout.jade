//- - var manifest = env == 'production' ? '/manifest.appcache' : false;
- var manifest = false;
doctype html
//[if lt IE 9]><html class="lt-ie10 lt-ie9"><![endif]
//[if IE 9]>   <html class="lt-ie10 ie9"><![endif]
//[if gt IE 9]><!
html(gengi-state='initial-loading' manifest=manifest)
  //<![endif]
  include _head

  body
    main
      block main

      if env !== 'production'
        .version=version

    block javascript
